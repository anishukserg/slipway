[package]
name = "slipway-plan"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "План работ самого Slipway, записанный по его же методологии"

# Документы плана лежат в корне крейта, без каталога src (решение 10).
[lib]
path = "lib.rs"

[lints]
workspace = true

[dependencies]
slipway-core = { workspace = true }
slipway-work = { workspace = true }
slipway-meta = { workspace = true }

[build-dependencies]
slipway-scan = { workspace = true }
